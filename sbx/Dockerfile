# Custom sbx template extending the default claude agent image
# Usage: sbx create -t agent-sandbox:latest claude .
# Build: docker build -t agent-sandbox:latest sbx/

# TODO: Determine the correct base image from sbx save
FROM ubuntu:24.04

# This Dockerfile will be extended in Issue #8 to:
# 1. Install claude CLI
# 2. Configure MCP proxy connection
# 3. Set up workspace mount points
